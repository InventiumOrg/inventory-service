# In each microservice repo
steps:
  - label: ":git: Clone centralized pipeline repo"
    command: |
      git clone --depth 1 https://github.com/InventiumOrg/buildkite-cicd.git /tmp/cicd
      ls -la .
      ls -la /tmp/cicd

  - wait

  - label: ":golangci-lint: Load code linter pipeline"
    command: |
      buildkite-agent pipeline upload .buildkite/templates/code-linter.yml

  - label: ":shield: Load security scan pipeline"
    command: |
      buildkite-agent pipeline upload .buildkite/templates/security-scan.yml

  - label: ":docker: Load image build pipeline"
    command: |
      buildkite-agent pipeline upload .buildkite/templates/image-build.yml
